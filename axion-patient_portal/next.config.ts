import { NextConfig } from "next";

const nextConfig: NextConfig = {
    // async rewrites() {
    //     return [
    //         {
    //             source: "/api/reports/graphql", // The endpoint your frontend will call
    //             destination: "https://axion.com/reports/graphql", // External API URL
    //         },
    //     ];
    // },
};

export default nextConfig;
